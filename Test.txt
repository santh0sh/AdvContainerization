<build>
    <plugins>
        <plugin>
            <groupId>com.alterego.oss.maven.plugin</groupId>
            <artifactId>yuicompressor-maven-plugin</artifactId>
            <version>1.5.0</version>
            <executions>
                <execution>
                    <goals>
                        <goal>compress</goal>
                    </goals>
                    <configuration>
                        <sourceDirectory>${project.basedir}/src/main/webapp/js</sourceDirectory>
                        <outputDirectory>${project.build.directory}/minified-js</outputDirectory>
                        <includes>
                            <include>**/*.js</include>
                        </includes>
                    </configuration>
                </execution>
            </executions>
        </plugin>
    </plugins>
</build>
