@Repository
public interface PressReleaseRepository extends JpaRepository<PressReleaseEntity, String> {

    // Fetch press releases based on category ID using method naming convention
    List<PressReleaseEntity> findByCategoryMappings_CategoryEntity_CategoryId(String categoryId);
}
